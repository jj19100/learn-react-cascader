import styled from "styled-components";

export const Wrapper = styled.div`
  .antdm-cascader-modal {
    height: auto !important;
  }
  .antdm-cascader-modal .am-modal-body {
    font-size: inherit;
    color: inherit;
    height: inherit;
    line-height: inherit;
    overflow: inherit;
    text-align: left;
  }
`